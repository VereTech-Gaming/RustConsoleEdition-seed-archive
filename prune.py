"""This script is to be run directly.
It will iterate through all /live seeds, and attempt to compress
any image with a filename sharing the current seed number.
For example, if the iterator scans directory /5532,
it will attempt to compress image file /5532/5532.png."""